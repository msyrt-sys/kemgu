// D-30x: görev_başlat CAPTURE — heap env; sonuç<görev<T>, metin> açılır. Exit: 42
işlev main() -> tam32 {
    değişken temel: tam32 = 40;
    eşleş görev_başlat(|| temel + 2) {
        tamam(g) => { ver görev_birleştir(g); }
        hata(e) => { ver 1; }
    }
    ver 99;
}
