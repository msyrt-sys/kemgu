// TC5d: bit operatörü mantıksal operandla → T028 (operatör konumu).
işlev main() -> tam32 {
    değişken m: mantıksal = doğru;
    değişken r: tam32 = 5 & m;   // tam & mantıksal → T028
    ver r;
}
